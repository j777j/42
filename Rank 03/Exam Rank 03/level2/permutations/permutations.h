#ifndef PERMUTATIONS_H
# define PERMUTATIONS_H

# include <stdio.h>  // for puts
# include <stdlib.h> // for malloc, free

// 由于这是一个独立的程序，所有辅助函数（如 generate_permutations, sort_string）
// 都应该在 .c 文件中声明为 static。
// 因此，这个头文件中不需要包含任何函数原型。

#endif
